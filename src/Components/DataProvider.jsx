import { useEffect } from "react";
import { dressList } from "../Components/ApiFetch";
import { subCategories } from "./TypeConstants";


export {ProductByGender};




 // genders.forEach((gender) => {
            //     categoryByGender[gender] = {};
            //     subCategories.forEach((subCategory) => {
            //         const filteredProducts = result.filter((product) => (
            //             product.gender === gender && product.subCategory === subCategory
            //         ));
            //         categoryByGender[gender][subCategory] = filteredProducts;
            //     });
            // });
